# auth-barebones
A MEAN stack template with authentication

# extra depedencies
```bash
sudo npm install -g nodemon
```

# dependencies of project itself
```bash
cd <project_root>
npm install
```

# run
```bash
cd <project_root>
nodemon
```
